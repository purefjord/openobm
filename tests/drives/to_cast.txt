# Drive to in-level gameplay, THEN open the pause menu (key 21) so the game loop /
# AI sub-loop is suspended and the shared combat RNG (b.var_java_util_Random_a) is
# idle while dumpCast seeds + invokes it. dumpCast swaps a synthetic 25-slot actor
# array into b.var_j_arr_a and, for the wander scenario, a synthetic open map into
# b's layer vector / collision layer / stride (all restored after), then drives the
# real per-actor tick h.a(j,long,boolean) through the armed/creature spell branch.
wait 19000
tap fire
wait 4000
tap fire
wait 4000
tap fire
wait 40000
tap 21
wait 2500
dumpcast cast_real.txt
